<%= form_for @main_character, :url => main_characters_path, :html => { :multipart => true } do |f| %>
  <% if @main_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@main_character.errors.count, "error") %> prohibited this main_character from being saved:</h2>

      <ul>
      <% @main_character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
