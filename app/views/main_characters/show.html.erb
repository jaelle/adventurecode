<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @main_character.title %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @main_character.image.url(:thumb) %>
</p>

<%= link_to 'Edit', edit_main_character_path(@main_character) %> |
<%= link_to 'Back', main_characters_path %>
