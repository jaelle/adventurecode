<%= form_for(@puzzle) do |f| %>
  <% if @puzzle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@puzzle.errors.count, "error") %> prohibited this puzzle from being saved:</h2>

      <ul>
      <% @puzzle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.text_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :maze_id %><br>
    <%= f.text_field :maze_id %>
  </div>
  <div class="field">
    <%= f.label :unique_link %><br>
    <%= f.text_field :unique_link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
